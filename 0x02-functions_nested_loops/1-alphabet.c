#include "main.h"

/**
 * print_alphabet - utilizes on the _putchar function to print
 * the alphabet a - z
 * Return: 0
 */
void print_alphabet(void)
{
	char ch;
	
	for (ch = 'a'; <= 'z'; ++ch)
	{
		_putchar(ch);
	}
_putchar('\n');
return (0);

}
